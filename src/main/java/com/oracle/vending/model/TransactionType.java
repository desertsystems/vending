package com.oracle.vending.model;

/**
 * Transaction type enum
 * used used to denote transaction type on the client
 */
public enum TransactionType {
    INITIATE,
    DEPOSIT,
    REMOVE,
    RESET;
}
